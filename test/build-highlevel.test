#!/bin/sh
set -e

rm -Rf built-highlevel
mkdir -p built-highlevel

cd built-highlevel
../build-highlevel
cd ..

for input in input-clean/*; do
	output=built-highlevel/$(basename $input)
	cmp $input $output || echo "warning: $input mismatch" >&2
done
